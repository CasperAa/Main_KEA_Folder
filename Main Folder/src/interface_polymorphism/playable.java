package interface_polymorphism;

public interface playable {

    //Creating two abstract methods, one to capture a name of the instrument & the other a sound

    public String nameOfInstrument();
    public String playSound();
}
